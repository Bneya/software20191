<center>
<h1><%= @user.realname %></h1>
<h2><%= @user.username %></h2>
<h3>Contact: <%= @user.email %></h3>
<h5>Bio:  <%= @user.bio%> </h5>
<h6>Subscriptions</h6>
<p>
<strong>Subscribed to: </strong>
<%Suscription.where(user_id: @user.id).distinct.pluck(:course_id).each do |course| %>
<%= link_to "Curso %d"[course], courses_path(course) %>
<% end %>
</p>
<%= link_to  "Edit profile", edit_user_registration_path(@user) %>
</h5>
</center>