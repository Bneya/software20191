
<%= render @form_file, event: @event %>

<div class="text-center">
  <%= button_to 'Regresar a Horarios', schedule_path(day: @day, campus_id: @classroom.campus_id), class: "btn btn-primary" %>
</div>
